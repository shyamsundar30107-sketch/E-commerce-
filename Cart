<!DOCTYPE html>
<html>
<head>
    <title>Cart</title>
</head>
<body>
    <h1>Your Cart</h1>

    {% for item in cart_items %}
        <p>{{ item.name }} - ₹{{ item.price }}</p>
    {% endfor %}

    <h2>Total: ₹{{ total }}</h2>

    <a href="/">Back to Home</a>
</body>
</html>
